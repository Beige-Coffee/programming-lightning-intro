// use std::env;
// use std::io::prelude::*;
// use std::path::Path;
// use std::process::Command;

// fn main() {
//     let code = std::fs::read_to_string(&"./src/lib.rs").unwrap();
//     let mut file = std::fs::File::create("./src/syntax.txt").unwrap();
//     let syntax = syn::parse_file(&code).unwrap();
//     file.write_all(format!("{:?}", syntax).as_bytes());
// }

fn main() {}
